#ifndef SPLIT_HH
#define SPLIT_HH

#include <vector>
#include <string>
#include <algorithm>

using std::vector;
using std::string;
using std::find_if;

vector<string> split(const string& str);

#endif /* SPLIT_HH */
