#include <iostream>

using std::cout;
using std::endl;

// Done in ch15_pictures.cc.  Otherwise there would be no test code.

int main() {
    cout << "Starting." << endl;

    cout << "End." << endl;
    return 0;
}
