#include <iostream>
#include <string>
#include <vector>

using std::cout;
using std::endl;
using std::string;
using std::vector;

struct StudentInfo {
    string name;
    double midterm, final;
    vector<double> homework;
};

int main() {
    cout << "Starting." << endl;

    cout << "End." << endl;
    return 0;
}
